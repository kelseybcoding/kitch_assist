<%= form_for(kitchen_tool) do |f| %>
  <% if kitchen_tool.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kitchen_tool.errors.count, "error") %> prohibited this kitchen_tool from being saved:</h2>

      <ul>
      <% kitchen_tool.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :type %>
    <%= f.text_field :type %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
