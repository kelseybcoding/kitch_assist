<h1>Dashboard for <em><%=current_member.username %></h1>

		<% @recipes.each do |recipe| %>
	
		<h2><a href='/recipes/<%= recipe.id %>'><%= recipe.title %></a></h2>
		<p>Servings:<%= recipe.yield_number %> <%= recipe.yield_unit %></p>
			</div>
		</div>
		<% end %>
	</div>



